# I18 — React & TSX Parser Fixes

> **Origin**: [MAPX_MCP_FEEDBACK.md](../MAPX_MCP_FEEDBACK.md) — 2026-05-27 Post-Implementation feedback

| Field | Value |
|-------|-------|
| Status | `planned` |
| Started | — |
| Completed | — |
| Features | F34 |
| Branch | `feat/i18-react-tsx-parser-fixes` |
| PR | — |

---

## Scope

Fixes critical parser bugs preventing React and TSX files from being scanned, indexed, and queried correctly.

1. **F34 — React & TSX Parser Fixes** (🔴 CRITICAL):
   - Updates TS/TSX symbols query to extract default class/function exports and anonymous arrow functions exported as default.
   - Names anonymous default exports based on the filename of the source file.
   - Fixes the file walking / cache sync logic to ensure `.tsx` files are scanned and updated correctly (addressing the bug where `.tsx` files are omitted).

---

## Deliverables

| # | Deliverable | File(s) | Feature | Status |
|---|-------------|---------|---------|--------|
| 1 | Mappings for TSX in parser registry | `src/parsers/parser-registry.ts` | F34 | `planned` |
| 2 | Exclusions configuration adjustments | `src/core/config.ts` | F34 | `planned` |
| 3 | symbols.scm default export pattern mapping | `queries/typescript/symbols.scm` | F34 | `planned` |
| 4 | symbols.scm default export pattern mapping (JS) | `queries/javascript/symbols.scm` | F34 | `planned` |
| 5 | Anonymous default export auto-naming logic | `src/parsers/generic-wasm-parser.ts` | F34 | `planned` |

---

## Implementation Order

1. **TSX File walk validation** — verify `.tsx` files are successfully scanned in file-discovery.
2. **TypeScript symbol parser updates** — add tree-sitter default export matchers.
3. **Anonymous naming** — fallback to basename when a default export is anonymous.

---

## Acceptance Criteria

- [ ] `.tsx` files are processed and visible in `mapx files` output
- [ ] `export default function Pages()` is successfully indexed as a symbol
- [ ] `export default class App` is successfully indexed as a symbol
- [ ] Anonymous component defaults (e.g. `export default () => {}`) are named after the file name
- [ ] TypeScript compiles with 0 errors
