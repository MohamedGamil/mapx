# I02 — Glob Filter Pipeline in Scanner

| Field | Value |
|-------|-------|
| Status | `planned` |
| Started | — |
| Completed | — |
| Features | [F03](../features/F03-glob-filters.md) |
| Branch | `feat/i02-glob-filters` |
| PR | — |
| Blocked by | — |
| Blocks | — |

---

## Objective

Give users fine-grained control over which files are scanned and exported via `--include` / `--exclude` glob flags and persisted config-level defaults. Excluded files incur zero I/O cost.

This iteration is fully independent of I01 and I03 and can be developed and merged in parallel.

---

## Deliverables

| # | Deliverable | Owner | Status |
|---|-------------|-------|--------|
| 1 | `buildMatcher()` utility in `src/core/scanner.ts` using `minimatch` | — | `planned` |
| 2 | `--exclude` flag wired into `mapx scan` and `mapx update` | — | `planned` |
| 3 | `--include` flag wired into `mapx scan` and `mapx update` | — | `planned` |
| 4 | `--exclude` / `--include` flags wired into `mapx export` | — | `planned` |
| 5 | Config-level `excludePatterns` / `includePatterns` respected by default | — | `planned` |
| 6 | CLI flag patterns extend (not replace) config-level patterns | — | `planned` |
| 7 | `mapx status` output shows active patterns | — | `planned` |
| 8 | `mapx init` suggests language-appropriate defaults | — | `planned` |
| 9 | Dependency check: ensure `minimatch` is available (add to `package.json` if needed) | — | `planned` |

---

## Files expected to change

```
package.json               (minimatch dependency, if not already present)
src/core/scanner.ts        (buildMatcher, walkDirectory signature)
src/cli.ts                 (--exclude / --include flags on scan, update, export, status)
src/core/config.ts         (merge CLI patterns with config patterns)
src/exporters/llm-exporter.ts   (accept files filter from ExportOptions)
src/exporters/graph-exporter.ts
src/exporters/dot-exporter.ts
src/exporters/svg-exporter.ts
```

---

## Testing checklist

- [ ] `mapx scan --exclude "**/tests/**"` → test files absent from `mapx export` output
- [ ] `mapx scan --include "**/app/Services/**"` → only service files in output
- [ ] Comma-separated `--exclude "a/**,b/**"` and repeated `--exclude a/** --exclude b/**` both work
- [ ] Config-level patterns in `.mapx/config.json` applied without CLI flags
- [ ] CLI patterns extend (do not replace) config patterns
- [ ] `mapx status` lists active exclude/include patterns
- [ ] `npx tsc --noEmit` passes

---

## Definition of Done

All deliverables above are checked, tests pass, PR is reviewed and merged to `main`.
