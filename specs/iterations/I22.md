# I22 — CLI/MCP Search & Context Usability

> **Origin**: [MAPX_MCP_FEEDBACK.md](../MAPX_MCP_FEEDBACK.md) — 2026-05-27 Post-Implementation feedback

| Field | Value |
|-------|-------|
| Status | `planned` |
| Started | — |
| Completed | — |
| Features | F38 |
| Branch | `feat/i22-mcp-usability` |
| PR | — |

---

## Scope

Implements various improvements to CLI commands and MCP tool calls to enhance developer and LLM agent integration.

1. **F38 — MCP & CLI Usability Improvements** (🟠 HIGH):
   - Supports wildcard `*` or empty search terms in `mapx_search` to list all symbols in a file.
   - Standardizes the `kind` search parameter case-insensitively.
   - Correctly documents the required `task` parameter for `mapx_context` and refines relevance scoring logic.
   - Creates a detailed `instructions.md` guide for the MCP server schema.
   - Adds heuristic file-level summaries to `llm` export output.

---

## Deliverables

| # | Deliverable | File(s) | Feature | Status |
|---|-------------|---------|---------|--------|
| 1 | Standardize search kind casing and wildcard checks | `src/cli.ts` / `src/mcp.ts` | F38 | `planned` |
| 2 | Document task parameter and refine scoring | `src/core/context-builder.ts` | F38 | `planned` |
| 3 | Create detailed server usage guide | `.agents/rules/instructions.md` | F38 | `planned` |
| 4 | Add file role summaries to LLM exporter | `src/exporters/llm-exporter.ts` | F38 | `planned` |

---

## Implementation Order

1. **Wildcard & Kind Search filters** — update CLI and MCP handlers.
2. **Context Builder improvements** — adjust relevance scoring weights.
3. **Exporters & Instructions** — add file summaries and write the instructions file.

---

## Acceptance Criteria

- [ ] `mapx search "*"` returns all symbols in a targeted file
- [ ] Kind filters operate case-insensitively
- [ ] The `task` parameter is properly defined in the MCP schemas and `AGENTS.md`
- [ ] `instructions.md` exists and documents tool usage
- [ ] LLM exports list short summaries of each file's primary exports
- [ ] TypeScript compiles with 0 errors
