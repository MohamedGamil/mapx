# I01 — Schema Migration + Parser Edge Labelling

| Field | Value |
|-------|-------|
| Status | `planned` |
| Started | — |
| Completed | — |
| Features | [F01](../features/F01-edge-verifiability.md) |
| Branch | `feat/i01-edge-verifiability` |
| PR | — |
| Blocked by | — |
| Blocks | I03 (partial — `mapx metrics --verified-only`) |

---

## Objective

Introduce the concept of **edge verifiability** throughout the storage and parsing layers. After this iteration every edge in the graph carries a `verifiability` label (`verified` or `inferred`) that downstream features (metrics, exports) can filter on.

---

## Deliverables

| # | Deliverable | Owner | Status |
|---|-------------|-------|--------|
| 1 | SQLite migration: add `verifiability` column to `edges` | — | `planned` |
| 2 | Schema version bump + migration guard in `store.ts` | — | `planned` |
| 3 | `ExtractedReference.verifiability` optional field in `types.ts` | — | `planned` |
| 4 | PHP parser labels dynamic/untyped calls as `inferred` | — | `planned` |
| 5 | JS/TS parsers label untyped method calls as `inferred` | — | `planned` |
| 6 | Common-method filter list (`src/parsers/common-methods.ts`) | — | `planned` |
| 7 | Scanner write path passes `verifiability` through to `insertEdge` | — | `planned` |
| 8 | JSON exporter includes `verifiability` field per edge | — | `planned` |
| 9 | DOT/SVG exporters render inferred edges as dashed | — | `planned` |
| 10 | `mapx status` edge breakdown includes verified/inferred counts | — | `planned` |

---

## Files expected to change

```
src/types.ts
src/core/store.ts          (schema version, migration, insertEdge)
src/core/store-node.ts     (insertEdge SQL)
src/core/store-bun.ts      (insertEdge SQL)
src/core/store-interface.ts
src/core/scanner.ts        (write path)
src/parsers/languages/php.ts
src/parsers/languages/javascript.ts
src/parsers/languages/typescript.ts
src/parsers/common-methods.ts      (new file)
src/exporters/graph-exporter.ts
src/exporters/dot-exporter.ts
src/exporters/svg-exporter.ts
src/cli.ts                 (status output)
```

---

## Testing checklist

- [ ] Fresh `mapx init` + `mapx scan` on a PHP Laravel project → verify inferred edges appear in `mapx export --format=json`
- [ ] Fresh `mapx init` + `mapx scan` on a TypeScript project → same
- [ ] Existing `.mapx/` database (pre-migration) opened → schema migration runs without error
- [ ] `mapx export --format=dot` → inferred edges are dashed in rendered output
- [ ] `npx tsc --noEmit` passes

---

## Definition of Done

All deliverables above are checked, tests pass, PR is reviewed and merged to `main`.
